<odoo>
	<record id="helpdesk_ticket_view_tree" model="ir.ui.view">
		<field name="name">helpdesk.ticket.tree</field>
		<field name="model">helpdesk.ticket</field>
		<field name="arch" type="xml">
			<tree string="Tickets">
				<field name="sequence" widget="handle"/>
				<field name="name"/>
				<field name="user_id"/>
				<button name="update_description" string="Update Description" type="object" class="oe_highlight"/>
				<button name="update_all_descriptions" string="Update All Descriptions" type="object" class="oe_highlight"/>
				<field name="description"/>
				<field name="state"/>
				<field name="date" optional="show"/>
				<field name="date_limit" optional="hide"/>
			</tree>
		</field>
	</record>
	
	
	<!-- 
		## En la vista formulario:

		poner un header con el status bar
		nombre con h1 como en pedido de venta
		dos columnas:
		- fecha, fecha límite
		- asignado, tiempo dedicado

		solapas:
		- Descripción
		- Acciones a realizar
	 -->

	<record id="helpdesk_ticket_view_form" model="ir.ui.view">
		<field name="name">helpdesk.ticket.form</field>
		<field name="model">helpdesk.ticket</field>
		<field name="arch" type="xml">
			<form string="Ticket">
				<header>
					<field name="state" widget="statusbar" statusbar_visible="new,assigned,pending,resolved" />
				</header>
				<sheet>
					<div class="oe_title">
						<h1>
							<field name="name"/>
						</h1>
					</div>
					<group>
						<group>
							<field name="date"/>
							<field name="date_limit"/>
							<field name="amount_time" widgets="float_time"/>
							<field name="tag_name"/>
						</group>
						<group>
							<field name="is_assigned"/>
							<field name="user_id"/>
							<field name="tickets_count"/>
							<field name="person_id" options="{'no_quick_create': True}"/>
							<field name="tag_ids" widget="many2many_tags"/>
							<button name="create_tag" string="Crear" type="object" class="oe_highlight"/>
							<button name="clear_tags" string="Limpiar" type="object" class="oe_highlight"/>
						</group>
					</group>
					<notebook>
						<page string="Description" name="description">
							<!-- Crear un boton en el ticket que actualize la descripción, y ponga un valor x.  -->
							<button name="update_description" string="Update Description" type="object" class="oe_highlight"/>
							<button name="update_all_descriptions" string="Update All Descriptions" type="object" class="oe_highlight"/>
							<field name="description"/>
						</page>
						<page string="To Do" name="todo">
							<field name="actions_todo"/>
						</page>
						<page string="Actions" name="actions">
							<button string="Set all as done" name="set_actions_as_done" type="object"/>
							<field name="action_ids">
								<tree string="Actions">
									<field name="name"/>
									<field name="state"/>
								</tree>
								<form>
									<button string="Set done" name="set_done" type="object" class="btn-primary"/>
									<button string="Set todo" name="set_todo" type="object" class="btn-primary"/>
									<group>
										<field name="name"/>
										<field name="state" readonly="1"/>
									</group>

								</form>
							</field>
						</page>
					</notebook>
				</sheet>
			</form>
		</field>
	</record>

	<!-- Vista kanban:
    mostrar en tarjeta usuario y tiempo dedicado
    agrupar por usuario
    extra: añadir un field color, para cambiar el color de la tarjeta, ver como está hecho en crm.lead -->
    <record id="view_helpdesk_ticket_kanban" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.kanban</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <kanban  default_group_by="user_id">
                <field name="name"/>
                <field name="date"/>
                <field name="color"/>
                <field name="user_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="#{!selection_mode ? kanban_color(record.color.raw_value) : ''} #{lost_ribbon ? 'oe_kanban_card_ribbon' : ''} oe_kanban_global_click oe_kanban_card d-flex flex-column">
                            <div class="o_dropdown_kanban dropdown">
                                <a class="dropdown-toggle o-no-caret btn" role="button" data-bs-toggle="dropdown" data-bs-display="static" href="#" aria-label="Dropdown menu" title="Dropdown menu">
                                    <span class="fa fa-ellipsis-v"/>
                                </a>
                                <div class="dropdown-menu" role="menu">
                                    <t t-if="widget.editable"><a role="menuitem" type="edit" class="dropdown-item">Edit</a></t>
                                    <t t-if="widget.deletable"><a role="menuitem" type="delete" class="dropdown-item">Delete</a></t>
                                    <ul class="oe_kanban_colorpicker" data-field="color"/>
                                </div>
                            </div>
                            <div class="oe_kanban_content flex-grow-1">

                                <strong class="o_kanban_record_title oe_partner_heading"><field name="name"/></strong>
                                <ul>
                                    <li><field name="date"/></li>
                                </ul>
                            </div>
                            <div class="oe_kanban_footer">
                                <div class="o_kanban_record_bottom">
                                    <!-- <div class="oe_kanban_bottom_left">
                                        <field name="priority" widget="priority" groups="base.group_user"/>
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div> -->
                                    <div class="oe_kanban_bottom_right">
                                        <field name="user_id" widget="many2one_avatar_user"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

	<!-- Añadir vista pivot:
		tiempo dedicado por usuario/mes
￼    -->
	<record id="view_helpdesk_ticket_pivot" model="ir.ui.view">
		<field name="name">view.helpdesk_ticket.pivot</field>
		<field name="model">helpdesk.ticket</field>
		<field name="arch" type="xml">
			<pivot string="Tickets">
				<field name="user_id" type="row"/>
				<field name="date" interval="month" type="col"/>
				<field name="amount_time" type="measure"/>
			</pivot>
		</field>
	</record>


	<!-- Añadir vista graph:
		tiempo dedicado por usuario
￼    -->
	<record id="view_helpdesk_ticket_graph" model="ir.ui.view">
		<field name="name">view.helpdesk_ticket.graph</field>
		<field name="model">helpdesk.ticket</field>
		<field name="arch" type="xml">
			<graph string="Tickets" type="line">
				<field name="user_id"/>
				<field name="amount_time" type="measure"/>
			</graph>
		</field>
	</record>

	<!-- Añadir vista calendar:
		por fecha mostrando cada usuario registrado
￼    -->
	<record id="view_helpdesk_ticket_calendar" model="ir.ui.view">
		<field name="name">view.helpdesk_ticket.calendar</field>
		<field name="model">helpdesk.ticket</field>
		<field name="arch" type="xml">
			<calendar string="Tickets" mode="month" date_start="date" color="user_id" hide_time="true" event_limit="5">
				<field name="name"/>
				<field name="user_id" avatar_field="avatar_128"/>
			</calendar>
		</field>
	</record>

	<!-- Añadir vista search:
	￼    filtrar por nombre y por usuario asignado
	￼    filtrar tickets asignados
	￼    filtrar tickets sin date_due
	￼    agrupar por date_due
	￼    agrupar por usuario asignado
￼    -->
	<record id="view_helpdesk_ticket_search" model="ir.ui.view">
        <field name="name">view.helpdesk_ticket.search</field>
        <field name="model">helpdesk.ticket</field>
        <field name="arch" type="xml">
            <search string="Tickets">
                <field name="name"/>
                <field name="user_id"/>
                <filter string="Assigned" name="filter_to_assigned" domain="[('is_assigned','=',True)]"/>
                <filter string="No date" name="filter_no_date" domain="[('date','=',False)]"/>
				<filter string="Date" name="date" context="{'group_by':'date'}"/>
				<filter string="User" name="user" context="{'group_by':'user_id'}"/>

            </search>
        </field>
    </record>

	<record id="helpdesk_ticket_action" model="ir.actions.act_window">
		<field name="name">Helpdesk Tickets</field>
		<field name="type">ir.actions.act_window</field>
		<field name="res_model">helpdesk.ticket</field>
		<field name="view_mode">kanban,tree,form,pivot,graph,calendar</field>
		<field name="context">{}</field>
		<field name="help" type="html">
			<p class="o_view_nocontent_smiling_face">
				Create a new ticket
			</p>
		</field>
	</record>
	<menuitem id='helpdesk_menu' name='Helpdesk' sequence='10' web_icon="fa fa-star,#feb81c,#393939" />
	<menuitem id='helpdesk_ticket_open' name='Tickets' sequence='10' parent='helpdesk_menu' action='helpdesk_ticket_action' />
</odoo>